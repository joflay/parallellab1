#!/bin/bash

##NECESSARY JOB SPECIFICATIONS
#SBATCH --job-name=JobName       #Set the job name to "JobName"
#SBATCH --time=6:15:00           #Set the wall clock limit
#SBATCH --nodes=1              #Request nodes
#SBATCH --ntasks=2               #Request total tasks/cores
#SBATCH --ntasks-per-node=2      #Request 2 tasks/cores per node
#SBATCH --mem=16G                 #Request 32GB per node 
#SBATCH --output=output.%j       #Send stdout/err to "output.[jobID]" 
#
##OPTIONAL JOB SPECIFICATIONS
##SBATCH --mail-type=ALL              #Send email on all job events
##SBATCH --mail-user=email_address    #Send all emails to email_address 
#
##First Executable Line
#
matrix_size=$1
processes=$2
module purge
module load GCC/11.3.0
module load OpenMPI/4.1.4
module load GCCcore/12.2.0
module load PAPI/7.0.1
mpirun -np "$SLURM_NTASKS" ./mpi_mm "$matrix_size"

squeue -j $SLURM_JOBID
